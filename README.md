# ClipTen

一个只在本机运行的轻量 macOS 菜单栏剪贴板历史工具。

## 功能

- 自动记录最近 10 条纯文本剪贴板内容
- 相同内容自动去重，再次复制时移到最前
- 点击菜单栏图标查看，点选任一记录即可复制
- 首次启动或再次双击“应用程序”里的 ClipTen，会在鼠标附近弹出历史菜单；菜单栏图标被挤掉时也能使用
- 菜单关闭时也可使用全局快捷键 `⌃⇧1`～`⌃⇧0`（Control + Shift + 数字，共三键）
- 历史记录退出、重启和覆盖升级后仍保留在本机
- 支持清空历史；不显示 Dock 图标
- Finder 与 Launchpad 使用和菜单栏一致的原创剪贴板图标

## 安装

### 直接安装（推荐）

1. 打开项目的 [Releases 页面](https://github.com/ken0122/clipten/releases/latest)。
2. 在 **Assets** 中下载 `ClipTen-macOS.zip`。
3. 解压后，将 `ClipTen.app` 拖入 macOS 的“应用程序”文件夹。
4. 启动 `ClipTen.app`，菜单栏出现剪贴板图标即安装完成。

当前应用没有经过 Apple 公证。若首次启动时 macOS 提示无法验证开发者，请在 Finder 中右键 `ClipTen.app`，选择“打开”，然后再次确认“打开”。

如果 Releases 页面暂时没有可下载文件，可以按下面的方式从源码构建。

### 从源码构建

需要 macOS 13 或更高版本，以及 Xcode Command Line Tools。

```bash
git clone https://github.com/ken0122/clipten.git
cd clipten
./scripts/build-app.sh
mv dist/ClipTen.app /Applications/
open /Applications/ClipTen.app
```

### 更新已有安装

1. 从菜单栏退出 ClipTen。
2. 下载并解压新版，将新的 `ClipTen.app` 拖到“应用程序”，选择替换旧应用。
3. 重新启动。原有最近 10 条记录及顺序会恢复，不需要重新导入。

历史独立保存在 macOS 用户偏好设置中，不在 `.app` 内。更新时只替换应用，不要使用会删除应用数据的卸载/清理工具，也不要删除 ClipTen 的偏好设置。更换 macOS 用户或电脑不会自动转移历史。

有历史时，启动不会把当时的系统剪贴板重新插入列表，以免升级重启挤掉旧记录。启动后的新复制仍正常去重并保留最近 10 条；主动“清空记录”仍会删除历史。

## 使用

1. 启动 `ClipTen.app`。
2. 正常复制任意文本。
3. 点击菜单栏中的剪贴板图标。
4. 点击一条历史记录，将其重新复制到系统剪贴板。

如果顶部图标被其他常驻应用挤掉，双击“应用程序”中的 `ClipTen.app` 即可在鼠标附近打开同一份历史菜单。首次启动也会显示该菜单；按 Esc 或点击菜单外关闭，应用仍在后台记录。打开菜单本身不会修改历史或系统剪贴板。

也可以在任意应用中直接按 `Control + Shift + 数字`：`⌃⇧1`～`⌃⇧9` 对应第 1～9 条，`⌃⇧0` 对应第 10 条，无需先打开状态栏菜单。该组合避开了常见的 `⌘1`、`⌘⇧3` 等系统与应用快捷键；如果系统报告占用或注册失败，ClipTen 会跳过该键，并在菜单中显示不可用数字。个别应用的自定义快捷键仍可能使用相同组合，无法保证与所有软件完全无冲突。

记录通过 `UserDefaults` 保存在本机，不进行网络通信。

## 开发与升级兼容性

- 已发布的持久化域为 `local.luokun.ClipTen`，键为 `clipboardHistory`，格式为字符串数组；不要随版本号或应用显示名称改变它们。
- 升级只替换应用包，禁止在安装、启动或更新流程中清空用户偏好设置。未来如更换格式或存储位置，必须先验证旧数据迁移成功，再切换写入；不能用空列表覆盖未成功读取的数据。
- 每次更新运行 `swift test` 和 `./scripts/build-app.sh`。回归测试覆盖旧版 10 条记录的文本与顺序、启动不写回/不挤掉记录、下一次复制正常记录以及应用包标识兼容性。
- 测试只使用隔离的偏好设置域和剪贴板，不读取或清空真实用户数据。发布前还应在测试账户中安装旧版、复制 10 条、覆盖安装新版，核对内容与顺序。
- 另有真实 `.app` 包内的默认存储初始化测试，以仅存在于测试进程内的历史样本验证启动路径，防止测试宿主与应用包标识不同掩盖启动错误。

## 许可证

本项目采用 [MIT License](LICENSE)。
