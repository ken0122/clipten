# Product

<!-- impeccable:product-schema 1 -->

## Platform

macOS

## Users

单一用户在自己的 Mac 上工作时，需要快速找回刚刚复制过的文本或静态图片，不希望打开独立窗口或使用云同步。

以下未由用户逐项确认的细节均标记为本轮实现假设。

## Product Purpose

常驻 macOS 菜单栏，自动记录最近 10 条文字与静态图片（合计）；用户展开菜单点选，或用 Control + Shift + 数字将记录重新写回系统剪贴板并置顶，不自动粘贴。

## Positioning

只做最近 10 条本地历史，不创建账户、不上传数据、不占用 Dock，也不引入搜索、标签、图片浏览器或同步界面。

## Operating Context

应用作为菜单栏辅助工具持续运行。用户照常使用系统复制操作，需要回找时点击菜单栏图标。

## Capabilities and Constraints

- 记录纯文本剪贴板内容，忽略空白内容。
- 支持截图、浏览器和聊天软件提供的静态 PNG / TIFF，优先有效 PNG；同次复制只生成一条图片，不保留附带文字。
- 保留原始图片字节与透明通道，单张不超过 20,000,000 字节、4000 万像素；超限、损坏等在菜单和状态图标提示，不弹模态框。
- 文本按完整内容、图片按格式与字节摘要去重；再次复制移至最前，不承诺不同编码的同一画面合并。
- 最多保留文字与图片合计 10 条，并在应用退出、重启和覆盖升级后本地保留。
- 点选历史项或全局快捷键复制原始内容，成功后才置顶；异步复制绑定稳定 ID。
- 提供清空历史和退出应用操作。
- 清空令此前排队的复制失效，同时处理图片、索引和旧版备份；重启不得恢复已清空记录。
- 保持 Bundle Identifier 与固定 Application Support 目录；迁移旧文本后回读验证，保留旧键备份。索引缺失或损坏停止写入，单个图片缺失不删除其他记录。
- 不支持 Finder 文件复制、多图批量、动图、多页图片、OCR、云同步。
- 菜单栏图标被挤掉时，双击应用仍可在鼠标附近打开同一原生菜单。
- 实现假设：暂不默认设置为登录时启动。

## Product Principles

- 常驻但不打扰。
- 一次点击展开，一次点击复制。
- 数据只留在本机。
- 功能边界保持克制。

## Accessibility & Inclusion

使用系统原生菜单、系统字体、键盘菜单导航和 VoiceOver 可识别的标题。
