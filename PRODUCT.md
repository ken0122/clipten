# Product

<!-- impeccable:product-schema 1 -->

## Platform

macOS

## Users

单一用户在自己的 Mac 上工作时，需要快速找回刚刚复制过的文本，不希望打开独立窗口或使用云同步。

以下未由用户逐项确认的细节均标记为本轮实现假设。

## Product Purpose

常驻 macOS 菜单栏，自动记录最近 10 条纯文本剪贴板内容；用户展开菜单后可点选任一记录，将其重新写回系统剪贴板。

## Positioning

只做最近 10 条文本历史，不创建账户、不上传数据、不占用 Dock，也不引入复杂的搜索、标签或同步界面。

## Operating Context

应用作为菜单栏辅助工具持续运行。用户照常使用系统复制操作，需要回找时点击菜单栏图标。

## Capabilities and Constraints

- 记录纯文本剪贴板内容，忽略空白内容。
- 相同内容去重；再次复制会移动到最前。
- 最多保留 10 条，并在应用退出后本地保留。
- 点选历史项即可复制。
- 提供清空历史和退出应用操作。
- 实现假设：暂不默认设置为登录时启动。

## Product Principles

- 常驻但不打扰。
- 一次点击展开，一次点击复制。
- 数据只留在本机。
- 功能边界保持克制。

## Accessibility & Inclusion

使用系统原生菜单、系统字体、键盘菜单导航和 VoiceOver 可识别的标题。
