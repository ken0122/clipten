# 图片历史测试与验收

## 本轮验证结果（2026-08-27）

- Debug 与 Release 配置各通过 39 项 XCTest，无失败；包含真实 `.app` 默认初始化探针。
- Apple Silicon 本地应用包已通过 `codesign --verify --deep --strict`，Bundle Identifier 保持 `local.luokun.ClipTen`，签名为 ad-hoc，未公证；发布包另做 ZIP 解压和签名验证。
- `git diff --check` 通过；impeccable 源码检测无报告项，不能代替原生界面实测。
- 已按用户授权安装图片功能构建并启动，旧应用及数据保留本地备份；安装后逐条比对原有 10 条文本，内容和顺序完全一致。随后用户授权提交、推送与发布 v1.2.0。
- 开发者工具未完成真实跨应用粘贴、物理全局按键及视觉/无障碍验收，不能据发布授权推断这些检查全部通过，见下文。

## 自动测试（不操作正式历史或系统剪贴板）

```sh
swift test --scratch-path work/image-build
CLIPTEN_BUILD_DIR="$PWD/work/image-build" ./scripts/build-app.sh "$PWD/work/image-dist"
codesign --verify --deep --strict --verbose=2 work/image-dist/ClipTen.app
```

测试偏好设置域、存储目录、命名剪贴板均随机隔离，完成后删除测试自身创建的数据。真实 `.app` 初始化探针沿用发布 Bundle Identifier，但使用进程内的迁移标记和临时索引，避免向正式偏好设置写入。不要把测试用例改成 `.general`，也不要运行常规新版本来验证真实用户的迁移。

注意：UserDefaults 的 volatile domain 只遮盖读取，并不隔离写入。探针必须预置已迁移的临时索引，且存储器不得重复写入已完成的迁移标记。本轮首次探针运行曾误写这一新增标记，已仅撤回该标记并在后续测试复验其不存在；未写入正式历史键或创建正式新索引。

覆盖范围：

- PNG/TIFF 原字节与透明通道、48px 等比缩略图、图片优先于附带文字、PNG 无效时 TIFF 回退。
- 混合十条、去重置顶、文件淘汰、第十条快捷键处理函数、菜单稳定 ID、读取失败不修改剪贴板。
- 串行提交顺序、清空取消旧复制、外部新剪贴板取消慢复制、菜单重建不重新读取全尺寸图片。
- v1.1.0 字符串数组原序迁移、旧键保留、写盘失败、迁移中断、损坏/丢失索引只读保护、图片丢失、路径校验与符号链接。
- 清空备份标记、重启不重新捕获旧剪贴板、打包后的默认存储初始化。

超像素用例生成 8000×5001 的可压缩测试图片（准备夹具时有短暂内存开销），确保拒绝原因来自像素限制，而不是文件大小或伪造文件头。

## 人工验收清单（开发者工具未覆盖）

优先在独立 macOS 测试账户进行。该账户中先安装 v1.1.0，准备十条已知文本（中文、emoji、多行、前后空格），退出后覆盖测试构建，逐条核对内容和顺序。不在日常账户使用覆盖安装作测试。

1. 截图复制、浏览器“复制图片”、聊天软件“复制图片”各捕获一次静态图片；透明 PNG、TIFF 分别测试。
2. 菜单显示缩略图与正确尺寸。鼠标选中及真实 `Control + Shift + 1…0` 分别复制，在 Preview/文本编辑的富文本新文档中粘贴，核对完整图片、透明度及尺寸。目标应用不支持该格式时记录限制，不能算通过。
3. 文字与图片混排十条，重复复制置顶；核对每次置顶后快捷键对应的是新的位置。
4. 单纯打开菜单或再次打开应用不更改顺序、系统剪贴板；菜单栏图标不可见时双击应用仍能调出原生菜单。
5. 检查浅色/深色、键盘菜单导航和 VoiceOver；加载、空、超限、损坏及缺失图片状态不弹模态框，不循环通知，其他记录仍能操作。
6. 清空后重启，历史、图片、旧备份不会复活。存储故障只在测试账户的数据副本中模拟。

如必须在日常账户进行：

- 先记录正式实例 PID 和历史指纹，暂停该实例，不修改它的历史或偏好设置；另外运行使用临时目录、独立偏好设置域的验收实例。
- 在改动系统剪贴板前，逐条保存每个 `NSPasteboardItem` 的所有类型及原始数据，并校验捕获期间 `changeCount` 未变；有任意类型不能完整读取就取消实测。不可用 `pbpaste` 的纯文本备份代替。
- 原始剪贴板备份仅放本地内存或权限为 0600 的临时文件，不能写日志、提交仓库或上传。退出路径必须恢复所有条目与类型并逐字节比对，再恢复正式实例；清理临时备份。
- 记录正式历史指纹未变。测试完成前不要恢复正式实例，避免它捕获夹具。

当前工具环境缺少桌面控制能力，上述真实跨应用、真实全局按键及视觉/无障碍验收尚未执行。自动测试调用的是同一复制处理函数，不代表这些人工项已通过。打包校验只证明本地构建和 ad-hoc 签名完整，不是 Apple 公证，也不是安装或 Release 发布。
